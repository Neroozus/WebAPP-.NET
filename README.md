# WEB .NET
 
.NET CORE web application with CRUD operations, registration, authorization, email confirmation, admin panel, which uses:
Database (MSSQL), Entity Framework, JavaScript, Bootstrap, Identity
